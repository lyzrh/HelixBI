# Runtime Benchmark — Cold Sandbox vs Warm Pool

> 模式：`offline_simulation`，`measured=false`。池与并发调度是**真实代码**（租约 / 排队 / 超时 / 复用 / 上限），容器冷启动与执行耗时为**建模值**；真实 Docker 实测未采集。
> 建模常数：冷启动 1500 ms、执行 100 ms、池大小 2、每场景 16 个运行。

| 并发 | Cold p50 | Warm p50 | Cold p95 | Warm p95 | p50 提升 | 吞吐倍数 | 降级次数 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 1600 ms | 101 ms | 1601 ms | 102 ms | 93.7% | 15.94x | 0 |
| 5 | 1600 ms | 101 ms | 6402 ms | 809 ms | 93.7% | 7.90x | 0 |
| 10 | 1601 ms | 305 ms | 6402 ms | 810 ms | 81.0% | 7.88x | 0 |
| 20 | 4000 ms | 455 ms | 6402 ms | 808 ms | 88.6% | 7.90x | 0 |

## 池状态（负载结束后）

- 容器：warm 2（空闲 2 / 借出 0），累计创建 3、销毁 1（到期回收 1、异常替换 0）
- 复用执行 61 次；排队超时（池）0、等待者被拒 0
- 泄漏检查：`idle + leased == pool_size` → 通过

## 诚实性声明

- **真实代码**：SandboxPool 租约/排队/超时/复用/回收；RunRegistry 并发上限/单用户限制/队列
- **建模值**：docker run 冷启动耗时；代码执行耗时
- **未采集**：真实 Docker 冷启动 / 执行耗时（需 Docker 守护进程）。

